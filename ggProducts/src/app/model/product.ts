export class Product {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  product_id?: any;
  name: string;
  code: string;
  imgsrc?: string;
  category: string;
  isFavourite?: boolean;
}
